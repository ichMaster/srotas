# Phase v0.4 — Execution Report

**Date:** 2026-07-29
**Branch:** main
**Label:** v0::version:0
**Target release:** 0.4.0
**Executed by:** Claude Code (`/run-phase 0.4` → `/execute-issues`)

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

Phase v0.4 — **Web feed** — is complete. The scored items are now usable in a
browser: `web/app.py` (FastAPI + HTMX, bound to 127.0.0.1) renders the card feed
— fresh days on top, score-sorted within a day — with a clickable node-tag
filter, and an in-process APScheduler runs the collect → embed → score pass every
4 hours. 78 tests green, ruff clean repo-wide. **Phase boundary reached — no
release performed** (awaiting user review, then `/release-version 0.4.0`).

## Issues

| # | SROTAS ID | Title | Phase | Status | Commit | Files | Tests |
|---|-----------|-------|-------|--------|--------|-------|-------|
| 1 | SROTAS-013 | web/app.py — FastAPI+HTMX card feed (127.0.0.1), day-grouped, score-sorted | v0.4 | completed | 5f2a33d | 7 | pass |
| 2 | SROTAS-014 | Clickable node-tag filter (?node=, "всі" resets) | v0.4 | completed | 411abf9 | 4 | pass |
| 3 | SROTAS-015 | APScheduler inside the app — 4h collect→embed→score | v0.4 | completed | 7114d60 | 5 | pass |

Executed in dependency order: SROTAS-013 (the feed screen gate) → SROTAS-014
(filter) and SROTAS-015 (scheduler). Each issue = one commit, auto-closed via
`Closes #N` on push to `main`, with an implementation-summary comment.

## Detailed Results

### SROTAS-013: web/app.py — FastAPI+HTMX card feed (127.0.0.1)
**Status:** completed · **Commit:** 5f2a33d
**Files:** core/items.py, web/__init__.py, web/app.py, web/templates/feed.html, web/templates/_card.html, tests/test_web_feed.py, pyproject.toml
- `items.read_feed()` + `ScoredItem`; `GET /` groups items by day (published_at,
  fallback first_seen) fresh-first, score desc within a day; node tag = the
  `top_node`'s Ukrainian label. Binds to 127.0.0.1; inert feedback field.
**Validation:** [x] pytest (5) · [x] ruff · [x] TestClient · [x] acceptance

### SROTAS-014: Clickable node-tag filter
**Status:** completed · **Commit:** 411abf9
**Files:** web/app.py, web/templates/_card.html, web/templates/feed.html, tests/test_web_feed.py
- `GET /?node=<id>` narrows the feed to that node; "всі"/no param resets. Each
  card's tag links to its node filter; the reset link shows only when filtered.
**Validation:** [x] pytest (3) · [x] ruff · [x] TestClient · [x] acceptance

### SROTAS-015: APScheduler inside the app — 4h collect→embed→score
**Status:** completed · **Commit:** 7114d60
**Files:** core/config.py, config.example.toml, web/app.py, tests/test_scheduler.py, pyproject.toml
- `BackgroundScheduler` in the FastAPI lifespan runs `pipeline.run_pass` every
  `collection_interval_hours` (4 default); worker thread, non-blocking; graceful
  without config.toml. Added the interval to config + the apscheduler dep.
**Validation:** [x] pytest (4) · [x] ruff · [x] acceptance

## Contracts

None new this phase. The feed reads the existing Item schema and
`score`/`top_node` (v0.2/v0.3 contracts, unchanged). The 127.0.0.1 bind and the
"filter lives in the card tags" rule are honored (ARCHITECTURE §Feed).

## Scope discipline (MISSION §Scope boundaries — binding)

- **127.0.0.1 only** — the app never listens on an external interface.
- **Feedback field renders but posts nowhere** — the handler is phase 0.5.
- **No translation** — the scheduled pass is collect → embed → score; translation
  is phase 0.8 (not pulled in early).
- **One process** — the scheduler runs inside the app (no separate worker).
- No Wikipedia/GNews (0.6); no stubs or flags for later phases.

## Validation (phase-wide)

- `pytest` — **78 passed** (12 new this phase: 8 web feed/filter + 4 scheduler).
- `ruff check .` — clean repo-wide.
- All routes exercised with the FastAPI TestClient; the scheduler job tested with
  the pass mocked — no paid API or network in CI.
- **Real end-to-end smoke** (on the live `items.sqlite` + config): `with
  TestClient` booted the lifespan + scheduler, `GET /` rendered **128 cards
  across 85 day-sections**, `?node=science-fiction-literature` → **45 cards**
  with the "всі" reset, and the scheduler shut down cleanly.

## Next Steps

Phase complete — **awaiting user review**. When there are no objections:

```
/release-version 0.4.0
```

Then the next roadmap phase is **v0.5 — Feedback loop** (`/run-phase 0.5`, or the
generate → upload → execute skills). Translation (v0.8/v0.9) comes later, in
order.
