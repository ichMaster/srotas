# Phase v0.6 — Execution Report

**Date:** 2026-07-30
**Branch:** main
**Label:** v0::version:0
**Target release:** 0.6.0
**Executed by:** Claude Code (`/run-phase 0.6` → `/execute-issues`)

## Summary

| Status | Count |
|--------|-------|
| Completed | 3 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

Phase v0.6 — **Wikipedia + Google News collectors** — is complete. The feed now
draws on all three sources: Wikipedia (REST search + the daily featured
article) and Google News RSS (feedparser, en-US, one OR-joined request per
node) join the existing Guardian collector in `core/pipeline.run_pass`, all
writing into the same `items.sqlite`. Cross-source URL dedup needed no new
code — the existing URL-PK `upsert` already merges repeats regardless of which
collector wrote them. 127 tests green, ruff clean repo-wide. **Phase boundary
reached — no release performed** (awaiting user review, then
`/release-version 0.6.0`).

## Issues

| # | SROTAS ID | Title | Phase | Status | Commit | Files | Tests |
|---|-----------|-------|-------|--------|--------|-------|-------|
| 1 | SROTAS-019 | collectors/wikipedia.py — REST search + daily featured feed | v0.6 | completed | afcc7bc | 4 | pass |
| 2 | SROTAS-020 | collectors/gnews.py — Google News RSS, one OR-joined request per node | v0.6 | completed | 67414db | 3 | pass |
| 3 | SROTAS-021 | Wire both into run_pass + cross-source URL dedup | v0.6 | completed | 88a955d | 2 | pass |

Executed in dependency order: SROTAS-019 and SROTAS-020 (independent collector
modules, same shape as the v0.2 Guardian collector) → SROTAS-021 (joins both
into `run_pass`). Each issue = one commit, auto-closed via `Closes #N` on push
to `main`, with an implementation-summary comment.

## Detailed Results

### SROTAS-019: collectors/wikipedia.py — REST search + daily featured feed
**Status:** completed · **Commit:** afcc7bc
**Files:** collectors/base.py, collectors/guardian.py, collectors/wikipedia.py, tests/test_wikipedia.py
- `search()` — one MediaWiki REST v1 search request per node (OR-joined
  keywords); `featured()` — one request per collection run for the day's
  featured article, independent of any node (date injectable). Both normalize
  into `Item` (`source="wikipedia"`, `published_at=None`).
- Relocated `CollectSummary` to `collectors/base.py`, re-exported from
  `guardian.py` — no breakage (existing tests still resolve `guardian.CollectSummary`).
**Validation:** [x] pytest (8) · [x] ruff · [x] acceptance

### SROTAS-020: collectors/gnews.py — Google News RSS, one OR-joined request per node
**Status:** completed · **Commit:** 67414db
**Files:** collectors/gnews.py, tests/test_gnews.py, pyproject.toml
- `search()` — one Google News RSS request per node (OR-joined keywords,
  fixed `hl=en-US&gl=US&ceid=US:en`); response fetched over an injectable
  `httpx.Client` and parsed via `feedparser.parse(resp.content)` (**bytes**,
  never a URL — keeps the mockable HTTP seam intact). `normalize()` maps each
  entry → `Item` (`source="gnews"`).
- Added the `feedparser` dependency.
**Validation:** [x] pytest (5) · [x] ruff · [x] acceptance

### SROTAS-021: Wire both into run_pass + cross-source URL dedup
**Status:** completed · **Commit:** 88a955d
**Files:** core/pipeline.py, tests/test_pipeline.py
- `run_pass` now calls all three collectors into the same `items.sqlite`, each
  with its own injectable client. `PassSummary.collected` is now a
  `{"guardian", "wikipedia", "gnews"}` mapping of the shared `CollectSummary`.
- Cross-source dedup proven end to end (no new code needed — URL-PK upsert
  already merges repeats).
**Validation:** [x] pytest (3 new/updated) · [x] ruff · [x] acceptance

## Contracts

None new this phase. Both collectors normalize into the existing `Item` shape
(v0.2 contract, unchanged). `CollectSummary` was relocated (not reshaped) —
no test depended on its module path, only its fields, so nothing broke.

## Scope discipline (MISSION §Scope boundaries — binding)

- **Exactly two new collector modules** — `wikipedia.py` + `gnews.py`, same
  shape as Guardian; no plugin framework.
- **Dedup by URL only** — no simhash, no new dedup machinery.
- **`published_at` nullable** — Wikipedia items carry `None`; the feed day
  fallback to `first_seen` (already handled since v0.4) needed no changes.
- No HN/Reddit/arXiv/Telegram/YouTube (stage 5) pulled in early.

## Validation (phase-wide)

- `pytest` — **127 passed** (16 new this phase: 8 Wikipedia + 5 GNews + 3
  pipeline).
- `ruff check .` — clean repo-wide.
- All collector HTTP (Guardian/Wikipedia/GNews) and Voyage embeddings are
  mocked — never a paid or networked call in CI. `feedparser` only ever runs
  on already-mocked response bytes, never a live URL fetch.

## Next Steps

Phase complete — **awaiting user review**. When there are no objections:

```
/release-version 0.6.0
```

Then the next roadmap phase is **v0.7 — Full bootstrap** (`/run-phase 0.7`).
Translation (v0.8/v0.9) comes after that, in order.
