# Phase v0.2 — Execution Report

**Date:** 2026-07-22
**Branch:** main
**Label:** v0::version:0
**Target release:** 0.2.0
**Executed by:** Claude Code (`/execute-issues`)

## Summary

| Status | Count |
|--------|-------|
| Completed | 4 |
| Failed | 0 |
| Skipped | 0 |
| Remaining | 0 |

Phase v0.2 — **Guardian collector + items.sqlite** — is complete. Real articles
now land on disk: the config seam reads the Guardian key, the item store dedups
by URL, the collector issues one OR-joined request per node, and a manual CLI
run fills `items.sqlite` across the model. 40 tests green, ruff clean repo-wide.
**Phase boundary reached — no release performed** (awaiting user review, then
`/release-version 0.2.0`).

## Issues

| # | SROTAS ID | Title | Phase | Status | Commit | Files | Tests |
|---|-----------|-------|-------|--------|--------|-------|-------|
| 1 | SROTAS-005 | config.toml + core/config.py (Guardian key) | v0.2 | completed | 7a48757 | 3 | pass |
| 2 | SROTAS-006 | core/items.py — items.sqlite (WAL), Item, URL-dedup upsert | v0.2 | completed | 0b7d412 | 2 | pass |
| 3 | SROTAS-007 | collectors/guardian.py — one OR-joined request per node | v0.2 | completed | ebbbea6 | 4 | pass |
| 4 | SROTAS-008 | Manual CLI collect run across the 14 nodes | v0.2 | completed | 681a125 | 2 | pass |

Executed in dependency order: SROTAS-005 and SROTAS-006 (independent gates) →
SROTAS-007 (joins them + the v0.1 model reader) → SROTAS-008 (CLI over the
collector). Each issue = one commit, auto-closed via `Closes #N` on push to
`main`; an implementation summary was posted as a comment on each.

## Detailed Results

### SROTAS-005: config.toml + core/config.py (Guardian key)
**Status:** completed · **Commit:** 7a48757
**Files:** core/config.py, config.example.toml, tests/test_config.py
- Read-only `Config` over `tomllib`; fails loudly on missing file / missing key
  / empty key. `config.example.toml` documents the `[guardian] api_key` shape;
  `config.toml` stays gitignored. Only the Guardian key this phase (no
  placeholders for later-phase keys — MISSION §Scope boundaries).
**Validation:** [x] pytest (5) · [x] ruff · [x] py_compile · [x] gitignore · [x] acceptance

### SROTAS-006: core/items.py — items.sqlite (WAL), Item, URL-dedup upsert
**Status:** completed · **Commit:** 0b7d412
**Files:** core/items.py, tests/test_items.py
- `items.sqlite` (WAL) on the pinned 9-column schema; `Item` dataclass; `upsert()`
  dedups by URL, preserves `first_seen`, leaves `embedding`/`score`/`top_node`
  NULL for phase 0.3. **Contract test** pins the schema (url PK, published_at
  nullable) — ARCHITECTURE §Item store. `read_items()`/`count_items()` helpers.
**Validation:** [x] pytest (7) · [x] ruff · [x] py_compile · [x] contract · [x] acceptance

### SROTAS-007: collectors/guardian.py — one OR-joined request per node
**Status:** completed · **Commit:** ebbbea6
**Files:** collectors/__init__.py, collectors/guardian.py, tests/test_guardian.py, pyproject.toml
- `build_query()` OR-joins a node's keywords into one request; `normalize()` maps
  Guardian fields → `Item` (source=guardian); `collect()` upserts with dedup and
  returns a `CollectSummary`. httpx client injectable; all HTTP mocked via
  `httpx.MockTransport`. Added `httpx>=0.27`.
**Validation:** [x] pytest (5) · [x] ruff · [x] py_compile · [x] one-request-per-node · [x] acceptance

### SROTAS-008: Manual CLI collect run across the 14 nodes
**Status:** completed · **Commit:** 681a125
**Files:** collectors/guardian.py, tests/test_collect_cli.py
- `run_collect()` wires config → model → collect → `items.sqlite`;
  `python -m collectors.guardian` prints a fetched/new/deduped summary. No
  scheduler, no embedding pass. Integration test drives the full path with
  mocked HTTP, asserting cross-node URL dedup (5 fetched → 4 rows, 1 dedup) and
  NULL derived columns.
**Validation:** [x] pytest (2) · [x] ruff · [x] py_compile · [x] integration · [x] acceptance

## Contracts

- **Item shape / items schema** pinned this phase — `test_items.py::test_schema_is_the_nine_columns_contract` (matches ARCHITECTURE §Item store; no drift, no doc change needed).
- **Config seam** introduced (`tomllib` read-only) — no ARCHITECTURE contract change.
- No other stable seam touched; the v0.1 contracts (flat node format, minimal events schema) untouched.

## Scope discipline (MISSION §Scope boundaries — binding)

- Dedup by **URL only** — no simhash.
- **One OR-joined request per node** — not per keyword.
- `embedding`/`score`/`top_node` created but left **NULL** — no scoring/embeddings this phase.
- **No scheduler** — collection is a manual CLI run.
- No stubs or config flags for later phases; only the Guardian key added to config.

## Validation (phase-wide)

- `pytest` — **40 passed** (19 new this phase: 5 config + 7 items + 5 guardian + 2 CLI).
- `ruff check .` — clean repo-wide.
- No paid API (Voyage, Anthropic) touched; all Guardian HTTP mocked — nothing networked in CI.

## Next Steps

Phase complete — **awaiting user review**. When there are no objections, cut the
release:

```
/release-version 0.2.0
```

Then phase **v0.3 — Scoring + CLI preview** may be launched (its issues file
`v0.3-issues.md` already exists; upload with `/upload-issues` first).
